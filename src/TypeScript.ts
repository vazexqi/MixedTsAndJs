class TypeScript {
    name: string;

    constructor(name: string) {
        this.name = name;
    }

    /**
     * Some comments
     */
    aMethod(): void {
        console.log('aMethod');
    }
}